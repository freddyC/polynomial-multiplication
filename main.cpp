
#include "tools.h"
#include <iostream>

std::vector<double> simple (std::vector<double> a, std::vector<double>b);
std::vector<double> fourMult (std::vector<double> a, std::vector<double>b);
std::vector<double> threeMult (std::vector<double> a, std::vector<double>b);

const int P_SIZE = 2;
const int Q_SIZE = 2;

int main() {
  
  funcEval()

  return 0;
}